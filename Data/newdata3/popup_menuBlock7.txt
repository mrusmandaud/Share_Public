final android.util.DisplayMetrics popupMetrics =
        getResources().getDisplayMetrics();

final float popupDensity =
        popupMetrics.density;

final int popupWidth =
        (int) (220 * popupDensity);

final int buttonHeight =
        (int) (50 * popupDensity);

final int horizontalPadding =
        (int) (16 * popupDensity);

final int verticalPadding =
        (int) (8 * popupDensity);

final int cornerRadius =
        (int) (12 * popupDensity);


/*
 * ==========================================================
 * FIND WEBVIEW
 * ==========================================================
 */

android.webkit.WebView popupWebView = null;

if (
        binding.recordsContainer
                instanceof android.view.ViewGroup
) {

    android.view.ViewGroup container =
            binding.recordsContainer;

    for (
            int i = 0;
            i < container.getChildCount();
            i++
    ) {

        android.view.View child =
                container.getChildAt(i);

        if (
                child
                        instanceof android.webkit.WebView
        ) {

            popupWebView =
                    (android.webkit.WebView)
                            child;

            break;
        }
    }
}

if (popupWebView == null) {
    return;
}

final android.webkit.WebView finalWebView =
        popupWebView;


/*
 * ==========================================================
 * POPUP LAYOUT
 * ==========================================================
 */

final android.widget.LinearLayout popupLayout =
        new android.widget.LinearLayout(
                MyrecordsActivity.this
        );

popupLayout.setOrientation(
        android.widget.LinearLayout.VERTICAL
);

popupLayout.setPadding(
        horizontalPadding,
        verticalPadding,
        horizontalPadding,
        verticalPadding
);


/*
 * ==========================================================
 * BACKGROUND
 * ==========================================================
 */

final android.graphics.drawable.GradientDrawable
        popupBackground =
        new android.graphics.drawable.GradientDrawable();

popupBackground.setColor(
        android.graphics.Color.WHITE
);

popupBackground.setCornerRadius(
        cornerRadius
);

popupBackground.setStroke(
        (int) (1 * popupDensity),
        android.graphics.Color.LTGRAY
);

popupLayout.setBackground(
        popupBackground
);


/*
 * ==========================================================
 * COPY
 * ==========================================================
 */

final android.widget.Button copyButton =
        new android.widget.Button(
                MyrecordsActivity.this
        );

copyButton.setText("Copy");

copyButton.setTextSize(15);

copyButton.setTextColor(
        android.graphics.Color.BLACK
);

copyButton.setGravity(
        android.view.Gravity.CENTER_VERTICAL
);

copyButton.setAllCaps(false);

copyButton.setBackgroundColor(
        android.graphics.Color.TRANSPARENT
);

copyButton.setLayoutParams(
        new android.widget.LinearLayout.LayoutParams(
                -1,
                buttonHeight
        )
);

popupLayout.addView(
        copyButton
);


/*
 * ==========================================================
 * SHARE
 * ==========================================================
 */

final android.widget.Button shareButton =
        new android.widget.Button(
                MyrecordsActivity.this
        );

shareButton.setText("Share");

shareButton.setTextSize(15);

shareButton.setTextColor(
        android.graphics.Color.BLACK
);

shareButton.setGravity(
        android.view.Gravity.CENTER_VERTICAL
);

shareButton.setAllCaps(false);

shareButton.setBackgroundColor(
        android.graphics.Color.TRANSPARENT
);

shareButton.setLayoutParams(
        new android.widget.LinearLayout.LayoutParams(
                -1,
                buttonHeight
        )
);

popupLayout.addView(
        shareButton
);


/*
 * ==========================================================
 * EXPORT
 * ==========================================================
 */

final android.widget.Button exportButton =
        new android.widget.Button(
                MyrecordsActivity.this
        );

exportButton.setText("Export");

exportButton.setTextSize(15);

exportButton.setTextColor(
        android.graphics.Color.BLACK
);

exportButton.setGravity(
        android.view.Gravity.CENTER_VERTICAL
);

exportButton.setAllCaps(false);

exportButton.setBackgroundColor(
        android.graphics.Color.TRANSPARENT
);

exportButton.setLayoutParams(
        new android.widget.LinearLayout.LayoutParams(
                -1,
                buttonHeight
        )
);

popupLayout.addView(
        exportButton
);


/*
 * ==========================================================
 * SELECT ALL
 * ==========================================================
 */

final android.widget.Button selectAllButton =
        new android.widget.Button(
                MyrecordsActivity.this
        );

selectAllButton.setText("Select All");

selectAllButton.setTextSize(15);

selectAllButton.setTextColor(
        android.graphics.Color.BLACK
);

selectAllButton.setGravity(
        android.view.Gravity.CENTER_VERTICAL
);

selectAllButton.setAllCaps(false);

selectAllButton.setBackgroundColor(
        android.graphics.Color.TRANSPARENT
);

selectAllButton.setLayoutParams(
        new android.widget.LinearLayout.LayoutParams(
                -1,
                buttonHeight
        )
);

popupLayout.addView(
        selectAllButton
);


/*
 * ==========================================================
 * DESELECT ALL
 * ==========================================================
 */

final android.widget.Button deselectAllButton =
        new android.widget.Button(
                MyrecordsActivity.this
        );

deselectAllButton.setText("Deselect All");

deselectAllButton.setTextSize(15);

deselectAllButton.setTextColor(
        android.graphics.Color.BLACK
);

deselectAllButton.setGravity(
        android.view.Gravity.CENTER_VERTICAL
);

deselectAllButton.setAllCaps(false);

deselectAllButton.setBackgroundColor(
        android.graphics.Color.TRANSPARENT
);

deselectAllButton.setLayoutParams(
        new android.widget.LinearLayout.LayoutParams(
                -1,
                buttonHeight
        )
);

popupLayout.addView(
        deselectAllButton
);


/*
 * ==========================================================
 * POPUP WINDOW
 * ==========================================================
 */

final android.widget.PopupWindow popupWindow =
        new android.widget.PopupWindow(
                popupLayout,
                popupWidth,
                android.widget.LinearLayout.LayoutParams.WRAP_CONTENT,
                true
        );

popupWindow.setBackgroundDrawable(
        new android.graphics.drawable.ColorDrawable(
                android.graphics.Color.TRANSPARENT
        )
);

popupWindow.setOutsideTouchable(true);

popupWindow.setFocusable(true);

popupWindow.setElevation(
        (int) (8 * popupDensity)
);


/*
 * ==========================================================
 * COPY CLICK
 * ==========================================================
 */

copyButton.setOnClickListener(
        new android.view.View.OnClickListener() {

            @Override
            public void onClick(
                    android.view.View v
            ) {

                ButtonsController.copy(
                        MyrecordsActivity.this,
                        finalWebView,
                        popupWindow
                );
            }
        }
);


/*
 * ==========================================================
 * SHARE CLICK
 * ==========================================================
 */

shareButton.setOnClickListener(
        new android.view.View.OnClickListener() {

            @Override
            public void onClick(
                    android.view.View v
            ) {

                ButtonsController.share(
                        MyrecordsActivity.this,
                        finalWebView,
                        popupWindow
                );
            }
        }
);


/*
 * ==========================================================
 * EXPORT CLICK
 * ==========================================================
 */

exportButton.setOnClickListener(
        new android.view.View.OnClickListener() {

            @Override
            public void onClick(
                    android.view.View v
            ) {

                ButtonsController.export(
                        MyrecordsActivity.this
                );

                popupWindow.dismiss();
            }
        }
);


/*
 * ==========================================================
 * SELECT ALL CLICK
 * ==========================================================
 */

selectAllButton.setOnClickListener(
        new android.view.View.OnClickListener() {

            @Override
            public void onClick(
                    android.view.View v
            ) {

                finalWebView.evaluateJavascript(
                        "if(window._selectAllCells)"
                        + "{window._selectAllCells();}",
                        null
                );

                popupWindow.dismiss();
            }
        }
);


/*
 * ==========================================================
 * DESELECT ALL CLICK
 * ==========================================================
 */

deselectAllButton.setOnClickListener(
        new android.view.View.OnClickListener() {

            @Override
            public void onClick(
                    android.view.View v
            ) {

                finalWebView.evaluateJavascript(
                        "if(window._deselectAllCells)"
                        + "{window._deselectAllCells();}",
                        null
                );

                popupWindow.dismiss();
            }
        }
);


/*
 * ==========================================================
 * SHOW
 * ==========================================================
 */

android.view.View anchor =
        findViewById(
                R.id.recordsContainer
        );

if (anchor != null) {

    popupWindow.showAsDropDown(
            anchor,
            0,
            0
    );
}